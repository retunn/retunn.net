import { dev } from '$app/environment'

export const title = 'Retunn\'s homepage';
export const description = 'Currently compiling in parallel...';
export const url = dev 
  ? 'http://localhost:5173'
  : 'https://retunn.net';
